'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.spawnSync = spawnSync;
exports.spawn = spawn;

var _child_process = require('./async/child_process');

var async = _interopRequireWildcard(_child_process);

var _child_process2 = require('child_process');

var child_process = _interopRequireWildcard(_child_process2);

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

function toolchainPrefix(toolchain) {
  return toolchain === 'default' ? [] : ["+" + toolchain];
}

function spawnSync(tool, args) {
  let toolchain = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 'default';
  let options = arguments[3];

  return child_process.spawnSync(tool, toolchainPrefix(toolchain).concat(args), options);
}

function spawn(tool, args) {
  let toolchain = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 'default';
  let options = arguments[3];

  return async.spawn(tool, toolchainPrefix(toolchain).concat(args), options);
}